schema_version: "1.0"

name: "bytestrone-ef-migration-assessment-bundle"
version: "1.0.8"
description: "Consolidated single-step Codemod Insights metrics emitter scanning .NET solutions for all metrics under step analyze-dbcontext-and-orm-patterns."
author: "bijo abraham"
license: "MIT"

targets:
  languages:
    - "csharp"
    - "javascript"
    - "html"
    - "xml"

keywords: ["transformation", "migration", "assessment"]

registry:
  access: "private"
  visibility: "private"

capabilities: []

workflows:
  - name: main
    default: true
    path: workflow.yaml
